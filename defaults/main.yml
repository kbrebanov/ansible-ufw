---
# defaults file for ufw

ufw_ipv6: "yes"
ufw_logging: "off"
ufw_outgoing_policy: "allow"
ufw_incoming_policy: "deny"
ufw_routed_policy: "deny"
ufw_rules:
  - rule: "allow"
    proto: "tcp"
    src: "any"
    to_port: 22
